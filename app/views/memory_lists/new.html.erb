<% content_for :title, t("resources.lists.create_title") %>

<%= render "shared/app_header" %>

<main class="mx-auto max-w-3xl px-5 py-10 sm:px-6 lg:px-8">
  <h1 class="text-4xl font-black text-ink"><%= t("resources.lists.new") %></h1>
  <section class="mt-8 rounded-lg border border-line bg-white/80 p-6 shadow-card">
    <%= form_with model: @memory_list, class: "space-y-5" do |form| %>
      <%= form.text_field :title, placeholder: t("resources.lists.title_placeholder_long"), class: "w-full rounded-lg border border-line bg-cream px-4 py-3" %>
      <%= form.submit t("resources.lists.submit"), class: "cursor-pointer rounded-full bg-ocean px-6 py-3 font-extrabold text-white" %>
    <% end %>
  </section>
</main>
